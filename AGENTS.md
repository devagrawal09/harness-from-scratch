# Repo Instructions

This is a tiny OpenRouter CLI agent demo. Keep changes small, readable, and dependency-free.

When the user names a skill, call `load_skill` with that skill name before responding. Skills live in `skills/<name>.md`.
